---
type: llm
scope: transcript
---
The toggle is added into the app's existing call controls area (the component the app already uses for its control buttons), following the app's existing button component conventions - not a separate floating UI or a parallel controls bar.

---
type: llm
scope: transcript
---
The agent identifies the real cause: polls are disabled on the `messaging` channel type (a server-side channel-type setting), and fixes or explains it at that level (enabling polls on the channel type via the API / getstream CLI, or telling the user to). A fix that only edits the button or client code fails.

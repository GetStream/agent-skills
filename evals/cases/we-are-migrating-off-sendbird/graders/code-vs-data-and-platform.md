---
type: llm
scope: last_message
---
The response distinguishes migrating the code (swapping the Sendbird SDK for Stream in the app) from migrating the data (users, channels, message history), and it asks which platform / SDK the app uses or looks for the project to find out.
